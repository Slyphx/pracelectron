## Desirable updates for the next version
- the left pane (which opens at youtube) should act like a browser, have a URL address bar
- the right pane should be capable at file handling, should be able to open files and folders, and list them like vscode does
  - you can achieve this by using monaco and implementing file handling
- it should have an embedded terminal
- application should be able to run or compile atleast one programming language, by sending compilation commands (i think theyre called POSIX) to the kernel
- it better compile LateX bruh
